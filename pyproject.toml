[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
norecursedirs = ["testdata", ".pytest_cache", "__pycache__"]

[project]
name = "drama-splitter"
version = "0.1.0"
requires-python = ">=3.12"
